# Configuration

## Permissions

Your GraphQL schema is only as safe as your permission model.

- Lock down WPGraphQL endpoint access if needed.
- Make sure only appropriate roles can query entries.

## Caching

For high-traffic sites, consider caching at the GraphQL layer.
